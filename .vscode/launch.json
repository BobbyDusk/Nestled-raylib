{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Nestled",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/Nestled",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "build",
            "env": {}
        }
    ]
}
