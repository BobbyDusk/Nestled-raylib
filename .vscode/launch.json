{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Nestled",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/Nestled",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "build",
            "env": {
                "ASAN_OPTIONS": "detect_leaks=0:fast_unwind_on_malloc=0"
            }
        }
    ]
}
